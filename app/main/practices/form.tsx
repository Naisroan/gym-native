import { Text } from "components";
import { Stack } from "expo-router";
import { Modal } from "react-native";

export default function PracticeFormPage() {
	return (
		<Text variant="subtitle">Ejercicio</Text>
	)
}