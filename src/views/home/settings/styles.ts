import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	titleText: {
		fontSize: 32,
		fontWeight: "700",
		marginBottom: 24
	},
	settingOption: {
		marginBottom: 12
	}
})